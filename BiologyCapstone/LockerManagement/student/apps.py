"""
Configures the app which makes holds the 
src folder for student.
"""

from django.apps import AppConfig

class StudentConfig(AppConfig):
    """
    Names the app student.
    """
    name = 'student'

